<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="subCategoryModel"
            type="com.creative.share.apps.heragelawal.models.SubCategoryDataModel.SubCategoryModel" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="8dp"
        android:background="@drawable/company_feature_row_bg"
        >

        <ImageView
            android:layout_width="50dp"
            android:layout_height="50dp"
            subCategoryIcon="@{subCategoryModel.icon}"
            android:padding="8dp"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:lines="2"
            android:layout_marginTop="5dp"
            android:layout_marginBottom="5dp"
            android:textColor="@color/black"
            android:textSize="14sp"
            android:text="@{subCategoryModel.title}"
            />
    </LinearLayout>
</layout>